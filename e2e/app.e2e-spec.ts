import { WtherPage } from './app.po';

describe('wTher App', () => {
  let page: WtherPage;

  beforeEach(() => {
    page = new WtherPage();
  });

  /*it('should display message saying app works', () => {
    page.navigateTo();
    expect(page.getParagraphText()).toEqual('app works!');
  });*/
});
